# Idle firmware: CPU sleeps forever and never touches GPIO, so the debugger
# (nrfjprog --memwr) can own the GPIO registers for LED/pin identification.
CONFIG_GPIO=n
CONFIG_SERIAL=n
CONFIG_CONSOLE=n
CONFIG_UART_CONSOLE=n
CONFIG_I2C=n
CONFIG_SPI=n
